/* int i = 0;
		if((*it)->size / npairs == 2)
		{
			while(i < ((*it)->size / 2))
			{
				(*dit)[d]->array[i] = (*it)->array[i];
				i++;
			}
			d++;
			while(i < (*it)->size)
			{
				(*dit)[d]->array[i] = (*it)->array[i];
				i++;
			}
			d++;
		} */
		/* else if((*it)->label[0] == "Non-participating")
		{
			if((*it)->size == npairs)
			{
				for(int i = 0; i < npairs; i++)
					(*dit)[d]->array[i] = (*it)->array[i];
				d++;
			}
			else if((*it)->size > npairs)
			{
				for(int i = 0; i < npairs; i++)
					(*dit)[d]->array[i] = (*it)->array[i];
				d++;
				int scsize = ((*it)->size - npairs);
				for(int i = 0; i < scsize; i++)
					(*dit)[d]->array[i] = (*it)->array[i];
				d++;
			}
			else
			{
				for(int i = 0; i < (*it)->size; i++)
					(*dit)[d]->array[i] = (*it)->array[i];
				d++;
			}		
		}
		it++;
		dit++; */
	/* std::list<PmergeMe**>::iterator itt = divide.begin();
	std::list<PmergeMe**>::iterator ett = divide.end();
	while(itt != ett)
	{
		std::cout << (*itt)[0]->array[0] << "//";
		itt++;
	}
	std::cout << std::endl; */





	//////////////////////////////////

